"""Runnable reference implementations for every module in the course.

Stdlib only. Each function and class here is exercised by
`examples/test_examples.py`, and the snippets embedded in `docs/` are excerpts
of this code -- so what you read in a module is code that actually passes tests.
"""
